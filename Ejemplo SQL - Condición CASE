/* Estructura CASE
CASE expresion
	When valor1 then instrucciones;
    When valor2 then instrucciones;
    When valor3 then instrucciones;
    ELSE instrucciones
END CASE;

CASE 
	When expresion1 then instrucciones;
    When expresion2 then instrucciones;
    When expresion3 then instrucciones;
    ELSE instrucciones
END CASE;
EJEMPLO CASE (Línea 19 al 35)
/*


DROP PROCEDURE insert_curso;
delimiter //
CREATE PROCEDURE insert_curso(p_codigo int, p_nombre varchar(50), p_precio decimal)
begin
case
    when p_precio<100 then
        select 'Precio no puede ser inferior a 100';
    when p_precio >300 then
        select 'Precio no puede ser superior a 300';
    else
        insert into cursos values(p_codigo,p_nombre,p_precio);
    end case;
end //
delimiter ;

call insert_curso(134,'Curso130',151);
select * FROM CURSOS;
